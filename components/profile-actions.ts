/** Header controls follow ProfileHeading's container, not the viewport. */
export const PROFILE_ACTION_CLASS = "pointer-coarse:h-11";

/** Secondary actions show only their icon until the header has room for every label. */
export const PROFILE_ACTION_LABEL_CLASS = "@max-4xl:sr-only";
